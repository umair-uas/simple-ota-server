# OTA Server Configuration

# URL devices use to download bundles
# Must be reachable from your IoT devices
SERVER_URL=https://your-server-ip:8443

# RAUC compatible string
# Must match the 'compatible' value in your device's /etc/rauc/system.conf
COMPATIBLE=iot-gateway-raspberrypi5
